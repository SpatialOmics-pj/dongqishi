source "https://rubygems.org"

gem "github-pages", group: :jekyll_plugins
gem "jekyll-remote-theme"
gem "webrick", "~> 1.8"

# Keeps local preview compatible with the macOS system Ruby; the validation
# workflow uses Ruby 3.1 and accepts the same lockfile.
gem "nokogiri", "~> 1.13.10"
